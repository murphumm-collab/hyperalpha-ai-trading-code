# HyperAlpha AI Trading V1 验收清单

日期：2026-06-12

## V1 目标

V1 不是让 AI 直接实盘下单。

V1 的完成标准是：用户可以在本地/测试环境通过 AI Trading 页面完成标的选择、策略草案、自然语言调整、保存、审批、回测证据绑定、信号事件生成、拒绝/确认 handoff，并且只有通过安全检查的信号才会交给外部订单后端。

真实交易执行仍由 HyperAlpha 订单后端负责。

## 必须完成

- 多用户隔离：每个用户只能读取、调整、审批、归档、生成和 handoff 自己的 strategy spec / signal event / handoff attempt。
- 多会话 Agent：AI Trading strategy spec、signal event、handoff attempt 必须支持当前用户内的 `agent_session_id` 分区；agent session 本身可创建、重命名、更新压缩上下文和归档，并能生成包含策略、信号、handoff attempt 摘要的非敏感 session context packet，避免一个用户的多个策略会话混在一起。
- 上下文压缩边界：agent session context packet 必须返回 requested/effective/max strategy/signal/attempt limits 和 `summary_max_chars`，agent-session list/CRUD/nested audit payload 必须返回 `context_summary_chars` 和 `summary_max_chars`，压缩摘要必须受限并且 over-limit 请求必须被 API 拒绝，避免单用户长上下文拖垮共享 DeepSeek/Qwen 模型。
- 手工记忆安全：用户手工创建/编辑 agent session `context_summary` 时必须在写入前拒绝 API key、token、secret、private key、password、authorization 等敏感文本；自动生成的压缩摘要和既有 strategy-spec 兼容路径仍必须保持非敏感/脱敏。
- 会话名称安全：用户手工创建/重命名 agent session 时必须拒绝 API key、token、secret、private key、password、authorization 等敏感名称；历史/脏数据里的 agent-session name 只能以 `[redacted_sensitive_session_name]` 进入 API response、嵌套 spec/signal JSON 和 DeepSeek/Qwen model-adjust 上下文。
- 策略名称安全：用户保存 strategy spec 时必须拒绝 API key、token、secret、private key、password、authorization 等敏感展示名；历史/脏数据里的 strategy spec name 只能以 `[redacted_sensitive_strategy_name]` 或通用名称脱敏标记进入 spec list/detail、嵌套 spec JSON 和 agent-session context packet。
- 策略文本/source 安全：strategy draft / adjust / model-adjust instruction、stop-loss/take-profit rule、model source、record source 和 spec metadata source 是用户/API 可控输入，新写入或进入模型 prompt 前必须拒绝 API key、token、secret、private key、password、authorization 或订单后端 URL；历史/脏 strategy text/source 只能在 strategy detail、signal preview/event 和 gateway payload 中以脱敏占位符返回，不能传给模型或订单后端。
- 响应层记忆安全：历史/脏数据里的 `context_summary` / `agent_context_summary` 即使含有 API key、token、authorization、private key 等敏感字样，也只能留在 DB 审计中，不能从 strategy spec、signal event、handoff attempt、agent-session list/context 或嵌套 spec/signal JSON response 返回。
- 前端提示词安全：`Load context` 把 agent-session context packet 放入聊天输入前必须再次做本地脱敏；策略、回测、信号和 handoff review prompt 把 JSON packet 放入聊天输入前也必须做本地脱敏，不能把未来后端/插件回归带来的 API key、token、authorization 或敏感 summary/error/prompt 文本送入模型上下文。
- 模型上下文信任边界：DeepSeek/Qwen model-adjust 使用 agent-session context 时必须明确标记为不可信用户记忆、只能作为参考，不能覆盖 system prompt、signal-only/no-order、回测 gate、硬风控或用户确认要求。
- 模型输出安全：DeepSeek/Qwen model-adjust 返回的 `instruction`、`rationale`、`risk_notes` 在应用、返回或持久化前必须清洗，secret-like 文本只能以 `[redacted_sensitive_text]` 返回，直接下单/市价单/限价单等执行措辞只能以 `[direct_order_intent_ignored]` 返回；前端当前策略卡片、review packet 和 session history 必须显示非敏感、可读的安全提示，让用户知道模型输出曾被红线处理。
- 模型配置错误安全：AI Trading 的 Model setup 入口复用 Hyper AI LLM Config modal 时，保存/连接测试失败只能显示白名单状态或安全 fallback，不能把 provider response body、后端 raw `detail`、连接异常文本或 thrown message 渲染给 To C 用户。
- 标的物加载错误安全：AI Trading market selector 加载 watchlist、market-universe、available-symbols 时必须按来源降级；只有三个来源都不可用才显示安全错误，且不能把 raw backend detail、provider/upstream error、fetch exception 或 thrown message 渲染给 To C 用户。
- 标的物文本安全：AI Trading market-universe 后端、前端 selector、chat prompt 入口和 strategy-spec draft 入口都必须过滤 malformed/过长/secret-like symbol；HIP-3 `dex:symbol` 必须保留，过滤后的安全 symbol 才能进入 UI、模型上下文和 draft 请求。
- 市场标的：Hyperliquid Crypto Top 20/50 与 HIP-3 Top 20/50 可用于策略入口，HIP-3 `dex:symbol` 身份不能丢失。
- 策略草案：支持结构化 draft、validate、save、list、detail、approve、archive。
- 策略调整：支持本地受控自然语言 adjustment，也支持 DeepSeek/Qwen model-adjust 后进入同一个安全 parser。
- 安全边界：任何 strategy spec / signal candidate 都必须保持 `signal_only`、`not_an_order`、`ai_may_place_orders=false`、`order_backend_only=true`。
- 回测前置：signal event / handoff 必须绑定 handoff-ready backtest evidence；策略调整后旧 approval/backtest 必须失效。
- 回测证据安全：`backtest_id` / `run_id` / `source` / `notes` 是手工或后端绑定 evidence 的可控字段，新写入时必须拒绝 API key、token、secret、private key、password、authorization 或订单后端 URL；历史/脏 backtest evidence 只能在响应层以脱敏占位符返回，不能进入模型上下文、signal event response 或 gateway payload。
- 市场上下文安全：signal preview / signal event 的 `market_context.source`、`regime` 和数值字段是用户/API 可控输入，新写入时必须拒绝 API key、token、secret、private key、password、authorization 或订单后端 URL；历史/脏 market context 只能在 signal detail response 和 gateway payload 中以脱敏占位符或空数值返回，不能把敏感文本传给模型或订单后端。
- 信号审计：signal preview 必须持久化成 current-user signal event，支持 detail/list/reject；reject `reason` 是用户可控字段，写入 signal audit 前必须拒绝 API key、token、secret、private key、password、authorization 或订单后端 URL，历史/脏 `review.reason` 只能在响应层以脱敏占位符返回。
- 订单后端 handoff：默认 disabled；启用后也必须要求用户确认、未过期、信号身份一致、action/symbol 一致、回测合格、payload 契约稳定。
- handoff 用户确认安全：`confirmation_source` 是用户可控字段，写入 attempt audit 或 gateway payload 前必须拒绝 API key、token、secret、private key、password、authorization 或订单后端 URL；历史/脏 `user_confirmation.source` 只能在响应层以脱敏占位符返回。
- 脱敏：API response、gateway payload、handoff attempt、handoff `error_message`、handoff `confirmation_source` / `user_confirmation.source`、signal reject `reason` / `review.reason`、strategy spec text/source、backtest evidence `backtest_id` / `run_id` / `source` / `notes`、signal `market_context` 都不能泄露 API key、token、secret、private key、authorization 或真实订单后端 URL。
- 前端：`/app/ai-trading` 页面必须支持完整操作流，按钮状态必须以后端 preflight 为准。
- 运营 readiness：管理员必须能通过只读、无网络、无提交的 production readiness 报告看到 Auth/JWKS、订单后端 handoff、AI stream、硬风控、模型 key policy、handoff audit 和 agent-session context budget 的上线 blocker/warning；管理员还必须能读取 production evidence explain 清单来确认真实上线前缺哪些外部验收材料；普通用户不能访问；CLI production readiness 可显式打开 DB audit 以覆盖持久化 handoff/session 风险。
- 生产操作员预检：必须有一个只读 CLI 聚合 Git governance、本地 runtime、production readiness 和 completion/evidence 边界；默认缺真实生产 env / 外部 evidence 时必须保持 blocked，且不能调用模型、交易所、订单后端或 GitHub，也不能输出 env secret 值；最终 JSON 还必须递归清洗 secret-like 字符串，包括带 secret 的外部 evidence 文件路径。
- 文档：status、memory、gateway contract、V1 checklist 必须保持最新。
- Git：只在 `codex/ai-agent-multitenant-foundation` 分支本地提交；GitHub 上传按当前用户要求暂不处理。

## 当前已验证

- `scripts/local-dev/run_ai_trading_v1_local_acceptance.sh --confirm-local-mock-handoff`：一键本地 V1 验收通过，覆盖 local dev shell syntax、backend compile、220 条 AI Trading 回归、API-level smoke、live DeepSeek/Qwen model-adjust 默认阻断、默认生产 handoff/readiness gate 阻断、默认生产 DB-audit readiness gate 阻断、默认 production operator preflight 阻断、production operator preflight output redaction marker、local V1 completion boundary audit summary gate、production completion boundary audit 预期阻断、production evidence explain/template/initializer gates、production evidence summary/expiry/cutover/run-id/artifact/blocker/progress/dry-run safety gates、production evidence non-object dry-run safety、production evidence frontend error safety、frontend strategy-action error safety、frontend prompt-packet sanitizer、frontend model-config error safety、frontend market-universe error safety、frontend market symbol sanitizer、agent-session name safety、strategy-spec name safety、strategy text/source safety、handoff error-message safety、handoff confirmation-source safety、signal rejection reason safety、backtest evidence safety、market context safety、local acceptance macOS mktemp portability、frontend handoff error safety、frontend agent-session error safety、local supervisor fork-pressure resilience、env-check Docker probe fallback、local acceptance transient retry、runtime readiness retry grace、agent-session response context redaction、frontend session context prompt sanitizer、model-adjust untrusted context boundary、model-adjust output sanitizer、frontend model-adjust output safety、frontend validation warning labels、model readiness UI source guard、model readiness next actions、model setup shortcut、model setup runtime refresh、gateway mode guard、frontend build、local LaunchAgent runtime mirror sync、带 `--require-runtime-mirror-current` 的 runtime readiness 冷启动重试、live local mock handoff；本轮 runtime readiness 第 1-4 次等待 frontend/backend/mock gateway/backend 冷启动或可恢复 fork pressure，第 5/24 次返回 `ready=true`，`runtime_mirror.current=true`，Docker `required_for_readiness=false` 因 Postgres 已监听；最新证据为 spec `#146`、signal event `#144`、handoff attempt `#142`、gateway response `mock_accepted`、runtime `mode=http`、runtime `target_kind=local_mock`、`agent_sessions.total=131`、`handoff_attempts.total=142`、`model_adjustment.ready=false` 且 blocker 为 `model_profile_not_configured`，并返回安全 next action `Create a Hyper AI model profile with DeepSeek or Qwen before model-adjust.`。
- `cd backend && uv run python scripts/ai_trading_v1_completion_audit.py --strict-local`：V1 完成度边界审计通过，报告 `local_v1_accepted=true`、`ready_for_live_orders=false`、`github_upload=deferred_by_user_request`、`git_governance.status=accepted`、current branch `codex/ai-agent-multitenant-foundation`、`external_pending_count=7`；`--strict-production` 按预期 exit 1，防止把本地 V1 验收误判为生产实盘可上线。真实外部验收后可提供 `--production-evidence-file <sanitized-json>`，但只有 evidence 完整且显式追加 `--allow-live-ready-from-evidence` 才允许 `ready_for_live_orders=true`。
- `cd backend && uv run python scripts/ai_trading_v1_completion_audit.py --explain-production-evidence`：返回非敏感、逐项 production evidence 操作清单，包含 required schema fields、safe artifact-ref schemes、forbidden secret/raw-output values、operator guidance 和当前 blockers；未提供外部 evidence 时每个外部验收项都会显示 `external_evidence_item_not_provided`，即使 evidence 已完整但未显式 `--allow-live-ready-from-evidence`，仍保持 `ready_for_live_orders=false`。
- `cd backend && uv run python scripts/ai_trading_v1_completion_audit.py --production-evidence-file ../docs/hyperalpha/ai-trading-v1-production-evidence.template.json --strict-production`：按预期 exit 1，模板被识别为 evidence 文件但 `production_evidence.ready=false`、`accepted_count=0`、`required_count=7`，不能用于实盘切换。
- `cd backend && uv run pytest tests/test_ai_trading_v1_completion_audit.py tests/test_ai_trading_env_check.py tests/test_ai_trading_frontend_readiness_source.py tests/test_ai_trading_live_stack_acceptance.py tests/test_ai_trading_model_adjust_live_acceptance.py tests/test_ai_trading_production_readiness_check.py tests/test_ai_trading_production_operator_preflight.py tests/test_ai_trading_production_readiness_api.py tests/test_ai_trading_routes.py tests/test_ai_trading_mock_gateway.py tests/test_ai_trading_production_handoff_check.py -q`：220 条 AI Trading/completion boundary/env checker/frontend readiness source guard/live-stack runner/live model-adjust runner/production readiness/operator preflight/admin readiness API/mock gateway/production handoff gate 回归通过，新增覆盖 production operator preflight output redaction：secret-like external evidence file path 在最终 preflight JSON 中变成 `[redacted_sensitive_preflight_value]`，不回显原路径中的 `api_key` / secret token；并保留 production operator preflight 默认 blocked、strategy text/source safety、market context safety、backtest evidence safety、signal rejection reason safety、handoff confirmation-source safety、handoff error-message safety、strategy-spec name safety、local acceptance macOS mktemp portability、agent-session name safety、frontend market symbol sanitizer、frontend market-universe error safety、frontend model-config error safety、frontend prompt-packet sanitizer、frontend strategy-action error safety、frontend agent-session error safety、local acceptance transient retry、env-check Docker probe fallback、local supervisor fork-pressure resilience、frontend handoff error safety、runtime readiness retry grace、production evidence frontend error safety、non-object dry-run safety、secret-pattern count、不回显 Authorization/Bearer secret、runtime mirror freshness gate、agent-session context/redaction/model-adjust/gateway mode/env-check/readiness 等既有覆盖。
- Focused backtest evidence safety 已通过：`cd backend && uv run pytest tests/test_ai_trading_routes.py -q -k "backtest_summary_rejects_sensitive_evidence_fields or legacy_sensitive_backtest_evidence or signal_preview_response_redacts_attached_backtest_sensitive_fields"` 返回 3 passing tests；覆盖敏感 `backtest_id` / `source` / `notes` 写入前 400 拒绝且不改变 strategy spec，legacy polluted `backtest_id` / `run_id` / `source` / `notes` 在 strategy detail 和 signal event response 中脱敏且不修改 DB 审计 JSON。
- Focused completion-audit marker 已通过：`cd backend && uv run pytest tests/test_ai_trading_v1_completion_audit.py -q -k "backtest_evidence_safety or signal_rejection_reason_safety or status_progress_marker"` 返回 2 passing tests；缺少 `| AI Trading backtest evidence safety | Done |` 时 local completion audit 会 fail closed。
- Focused market context safety 已通过：`cd backend && uv run pytest tests/test_ai_trading_routes.py -q -k "market_context_rejects_sensitive_values or market_context_responses_redact_legacy_sensitive_values or signal_detail_and_gateway_payload_redact_sensitive_fields"` 返回 3 passing tests；覆盖敏感 `market_context.source` / `regime` / 数值字段写入前 400 拒绝且不创建新 signal event，legacy polluted market context 在 signal detail 和 gateway payload 中脱敏且不修改 DB 审计 JSON。
- Focused market context completion-audit marker 已通过：`cd backend && uv run pytest tests/test_ai_trading_v1_completion_audit.py -q -k "market_context_safety or backtest_evidence_safety or status_progress_marker"` 返回 2 passing tests；缺少 `| AI Trading market context safety | Done |` 时 local completion audit 会 fail closed。
- Focused strategy text/source safety 已通过：`cd backend && uv run pytest tests/test_ai_trading_routes.py -q -k "strategy_text_and_source or strategy_spec_responses_redact_legacy_sensitive_text"` 返回 2 passing tests；覆盖敏感 strategy draft text、model source、record source、spec metadata source、adjustment instruction/source 写入前拒绝，legacy polluted strategy text/source 在 strategy detail、signal preview/event 和 gateway payload 中脱敏且不修改 DB 审计 JSON。
- Focused strategy text/source completion-audit marker 已通过：`cd backend && uv run pytest tests/test_ai_trading_v1_completion_audit.py -q -k "strategy_text_source_safety or market_context_safety or status_progress_marker"` 返回 2 passing tests；缺少 `| AI Trading strategy text source safety | Done |` 时 local completion audit 会 fail closed。
- Focused production operator preflight 已通过：`cd backend && uv run pytest tests/test_ai_trading_production_operator_preflight.py -q` 返回 3 passing tests；`cd backend && uv run pytest tests/test_ai_trading_v1_completion_audit.py -q -k "production_operator_preflight or current_repo_completion_audit"` 返回 3 passing tests；`ai_trading_v1_production_operator_preflight.py --skip-local-runtime --strict` 在默认无真实生产 env / 无外部 evidence 时按预期 exit 1，输出 `completion:live_orders_not_ready` 和 `production_readiness:not_ready`，并保持 `secret_policy=no_model_calls_no_exchange_calls_no_order_backend_calls_no_github_calls_no_env_secret_values_returned`；secret-like evidence path 输出会被 `[redacted_sensitive_preflight_value]` 替换。
- `cd backend && uv run python scripts/ai_trading_v1_completion_audit.py --init-production-evidence-file <repo-external-json>`：可生成仓库外、待填写、无 secret pattern 的 production evidence 骨架；生成后 `production_evidence_ready=false`、`ready_for_live_orders=false`，repo 内路径或已存在输出默认拒绝。
- `cd backend && uv run python -m py_compile api/ai_trading_routes.py services/ai_trading_strategy_spec_service.py services/ai_trading_production_handoff_service.py services/ai_trading_production_readiness_service.py database/migrations/add_ai_trading_agent_session_fields.py scripts/ai_trading_v1_live_stack_acceptance.py scripts/ai_trading_v1_acceptance_smoke.py scripts/ai_trading_model_adjust_live_acceptance.py scripts/ai_trading_v1_env_check.py scripts/ai_trading_production_handoff_check.py scripts/ai_trading_v1_production_readiness_check.py scripts/ai_trading_v1_production_operator_preflight.py scripts/ai_trading_v1_completion_audit.py tests/test_ai_trading_env_check.py tests/test_ai_trading_live_stack_acceptance.py tests/test_ai_trading_model_adjust_live_acceptance.py tests/test_ai_trading_production_readiness_check.py tests/test_ai_trading_production_operator_preflight.py tests/test_ai_trading_production_readiness_api.py tests/test_ai_trading_routes.py tests/test_ai_trading_v1_completion_audit.py`：通过。
- `cd backend && uv run pytest tests/test_ai_trading_routes.py tests/test_ai_trading_v1_completion_audit.py tests/test_ai_trading_production_operator_preflight.py -q`：155 条 route/completion/preflight boundary regression 通过，覆盖 first-class agent session create/update/archive、手工 agent-session `context_summary` 和 `name` create/update 敏感内容写入前拒绝、strategy spec name/text/source 写入前敏感内容拒绝、legacy polluted strategy/text/source/signal/handoff/backtest/market-context 响应和 gateway payload 脱敏、local acceptance macOS mktemp portability、completion audit 新的 production operator preflight Done/output-redaction marker/gate、preflight 默认 blocked、secret-like evidence path 脱敏且不回显 env secret，以及既有 session/context/model-adjust/handoff 隔离与安全边界。
- AI Trading agent-session context packet 现在返回 requested/effective/max strategy/signal/attempt limits 与 `summary_max_chars=2000`；route regression 覆盖压缩摘要长度受限，以及 `strategy_limit=21` / `attempt_limit=101` 等 over-limit context/compress 请求返回 422。
- AI Trading agent-session list、create/update/archive response、strategy spec / signal event / handoff attempt nested `agent_session` payload、context packet、compress-context response 都返回 `context_summary_chars` 和 `summary_max_chars=2000`；route regression 覆盖正常 summary、current-user 主 session record 回填、压缩后长度刷新和 Bob 隔离。
- Agent session detail 页现在展示 `Context limits`、`returned / requested / max`、`Summary chars n / 2000` 和 `redacted, no credentials`；In-app Browser 已打开 `/app/ai-trading/sessions/ait%3Abtc%3A4d53a40f6789` 验证该只读面板渲染且无 visible error。
- `cd backend && uv run python scripts/ai_trading_v1_acceptance_smoke.py`：通过，覆盖 draft -> model-adjust -> save -> attach backtest -> approve -> reject signal -> confirmed mock handoff -> runtime。
- `cd backend && uv run python scripts/ai_trading_model_adjust_live_acceptance.py`：未带 `--confirm-live-model-call` 时按设计返回 `success=false` 并退出 1；该 runner 只允许本地 backend，不接受/打印 API key，不创建 signal event、不 handoff、不下单。
- `cd backend && uv run python scripts/ai_trading_v1_env_check.py --strict --require-runtime-mirror-current`：在 Docker/Postgres/mock gateway/backend/frontend 启动后返回 `ready=true`，并结构化输出/校验 runtime gateway `target_kind=local_mock`、空 `runtime_config_blockers`、非敏感 `runtime_model_adjustment`、current-user `runtime_agent_context_budget.secret_policy=counts_only_no_summary_text`、`runtime_mirror.current=true`、`secret_policy=metadata_only_no_env_or_credentials`，以及 ready-state `next_actions=["Local AI Trading V1 runtime is ready; continue with browser acceptance or the aggregate V1 local acceptance runner."]`；缺少 `agent_sessions.context_budget`、secret policy 不等于 counts-only、runtime mirror 元数据缺失或 source tree digest 不匹配时会 fail closed。
- 本轮 env-check runtime agent context budget gate 后已复跑一键本地验收，并重跑 `scripts/local-dev/install_launch_agent.sh` 同步 runtime mirror；runtime readiness 返回 `ready=true`，one-key handoff 前显示 `runtime_agent_context_budget.total=48`、`active=47`、`redacted_context_summary_count=1`，live mock handoff 后 runtime-after 更新到 strategy specs `63`、signal events `61`、agent sessions `48`、handoff attempts `59`；同步 LaunchAgent 后 strict env check 显示 `runtime_agent_context_budget.total=49`、`active=48`，且不返回 summary 原文。
- `cd backend && uv run python scripts/ai_trading_v1_live_stack_acceptance.py --confirm-local-mock-handoff`：已由一键本地验收脚本复跑通过，覆盖本地 LaunchAgent/live Postgres 栈 draft -> save -> attach backtest -> approve -> eligible signal -> confirmed mock handoff -> attempt audit -> runtime；最新 live mock handoff 证据为 spec `#146`、signal event `#144`、handoff attempt `#142`、gateway response `mock_accepted`。脚本默认拒绝无确认 handoff，只允许本地 URL，并要求 runtime gateway `target_kind=local_mock`。
- `cd backend && uv run python scripts/ai_trading_production_handoff_check.py --strict`：当前默认配置应拒绝生产 handoff；真实订单后端验收前必须通过该生产前检查，且不能使用 localhost/mock/placeholder URL。
- `cd backend && uv run python scripts/ai_trading_v1_production_readiness_check.py --strict`：当前默认配置应拒绝生产 readiness；真实上线前必须同时通过 Auth/JWKS、真实订单后端 handoff、AI stream 容量隔离、硬风控 TP/SL/notional/杠杆配置，并且不输出 token/API key 原文。`--include-db-audits` 可显式加入持久化 handoff attempt 与 agent-session context audit；DB 不可用时 fail closed，只返回错误类型，不返回连接串/异常原文；一键本地 V1 验收已纳入该 DB-audit blocker gate。
- `DATABASE_URL=<local Postgres alpha_arena URL> uv run python scripts/ai_trading_v1_production_readiness_check.py --include-db-audits`：本地只读 DB audit 成功返回 `handoff_audit.total=35`、`agent_session_context.total=25`、`over_budget_count=0`、`redacted_context_summary_count=1`，并继续因真实 Auth/JWKS/订单后端/硬风控未配置而 `production_ready=false`；输出不包含数据库 URL/password、gateway token 或 summary 原文。
- `/api/ai-trading/admin/production-readiness`：本地运行时匿名请求返回 401，临时 admin bearer 返回脱敏 readiness；当前本地 mock 配置下 `production_ready=false`，13 个 blockers 覆盖真实 Auth/JWKS、生产 handoff URL、硬风控 TP/SL/notional 等未配置项，且 `token_value_returned=false`。测试环境中 admin-only `handoff_audit` 会对 failed/blocked handoff attempts 给出 warning/counts/latest non-submitted 元数据，并证明不会返回 attempt authorization/body/API-key/token/raw error。
- `/api/ai-trading/admin/production-evidence-explain`：测试环境中 admin-only 请求返回 completion audit 的非敏感 production evidence explain 清单，包含 7 个外部验收 item、required schema fields、safe artifact-ref schemes、forbidden values、real-order-backend `external_evidence_item_not_provided` blocker；匿名请求返回 401、普通用户返回 403，且不会返回 `AI_TRADING_SIGNAL_GATEWAY_TOKEN`、DeepSeek/Qwen API key 或任意 env secret。
- Settings Admin AI Trading Production Evidence UI：已接入 `/api/ai-trading/admin/production-evidence-explain`，显示 accepted/required、evidence provided、live-order blocked、progress summary、next-required action summaries、root-level blockers、item ready/blocker、artifact-ref count 和 bounded operator guidance；source guard 证明页面只使用 `extractAiTradingProductionEvidenceExplain` 的安全投影和 `formatAiTradingProductionEvidenceBlocker` 可读标签，不渲染 production evidence file path、env token/API key 名或 raw `context_summary`。
- Settings Admin AI Trading Production Evidence Validation：已接入 `/api/ai-trading/admin/production-evidence-validate`，管理员可粘贴脱敏 evidence JSON 做 dry-run 校验；API 不接受文件路径、不持久化 evidence、不调用模型/交易所/订单后端、不解锁 live orders，UI 只显示 accepted/required、live-order blocked、evidence blockers 和 blocked item labels。
- Settings Admin AI Trading Production Evidence Template：已接入 `/api/ai-trading/admin/production-evidence-template`，管理员可把 pending、无 secret 的外部 evidence skeleton 加载进 dry-run 校验框，并看到 root-field guidance、root-level next actions、每个外部验收项的安全 template guidance / required summary terms / next-required actions；API 不接受/返回 evidence 文件路径、不持久化文件、不调用模型/交易所/订单后端，且始终保持 `ready_for_live_orders=false`。
- Settings Admin AI Trading Production Evidence Payload Bounds：dry-run API 在 schema validation/secret scan 前拒绝超过 40000 bytes 的 evidence JSON 和超过 14 个 `items` key 的 item storm；前端 textarea 同步限制 40000 字符，413 响应不回显请求中的 secret-like 文本。
- AI Trading Agent Session Response Context Redaction：API response 会在返回前重新清洗 legacy/脏 `context_summary` / `agent_context_summary`，覆盖 agent-session list/context、strategy spec list/detail、signal event list/detail、handoff attempts 和 nested spec/signal JSON；测试证明原始脏值仍留在 DB 审计 row 中但不会出现在响应里。
- AI Trading Frontend Session Context Prompt Sanitizer：Hyper AI `Load context` 会先对 session context packet 做本地 sanitizer，再 `JSON.stringify` 到聊天输入；敏感 key 返回 `***`，疑似敏感的 `context_summary`/`agent_context_summary` 返回 `[redacted_sensitive_context]`，正常 UI 展示不受影响。
- AI Trading Model-Adjust Context Trust Boundary：DeepSeek/Qwen model-adjust 的 `agent_session_context` 现在带 `trust_boundary=untrusted_user_memory` 和 reference-only `usage_policy`，prompt 明确该上下文不能覆盖 system prompt、signal-only/no-order boundary、backtest gate、hard risk limits 或 user confirmation requirements。
- Playwright smoke 已打开 `http://127.0.0.1:5174/#settings`，跳过本地 onboarding 且未输入 API key，Settings shell 正常渲染；本地未登录/auth-disabled 状态下 admin 区块隐藏仍属预期，真实 admin production evidence UI 可视化验收继续留在真实 Auth/admin 登录态外部验收。
- Admin readiness 现在包含 `agent_session_context` component：统计 session summary total/active/archived、with/empty summary、max chars、near/over-budget、redacted/sensitive-looking counts；over-budget 会阻断 production readiness，near/redacted/sensitive 会产生 warning/next action，并返回非敏感 latest over-budget/redacted/sensitive context locator（id、agent_session_id、status、chars）方便运营定位清理；API/CLI next actions 也会带同样的安全 locator 元数据，但不返回 summary 原文或 fake secret。Focused readiness API/CLI locator next-action 测试 2 条通过，聚合 AI Trading 回归 79 条通过。
- Admin readiness 对 failed/blocked handoff attempts 会追加 warning-specific next actions，提示运营查看 audit history、修复 gateway/order-backend failure、检查 stale signal/backtest/session/gateway blocker，并要求 retry 前重新做用户确认；focused readiness 测试 8 条通过，聚合 AI Trading 回归 59 条通过。
- Settings Admin AI Trading Production Readiness 面板已支持 `Handoff Audit` / `Agent Context` 可读组件名、failed/blocked handoff attempt warning、agent context budget warning、latest over-budget/redacted/sensitive context locator 可读标签和 `Top Warnings` 区块；`cd frontend && npm run build` 通过，剩余为既有 browser-baseline/Browserslist/chunk-size warning。
- 本轮 UI 更新后已重跑 `scripts/local-dev/install_launch_agent.sh` 同步 runtime mirror；首次 strict env check 命中 backend 冷启动，重试后 `ready=true`；In-app Browser 打开 `http://127.0.0.1:5174/#settings` 并跳过本地 onboarding 后确认 Settings shell 正常渲染、无 visible error，本地 auth disabled 下 Admin readiness 控件隐藏仍是预期。
- 后端运行时 handoff eligibility 也会拒绝未设置 `AI_TRADING_PRODUCTION_HANDOFF_APPROVED=true` 的外部订单后端 URL；本地 mock gateway 不要求生产审批，但 runtime/local live-stack/production readiness 仍要求 `AI_TRADING_SIGNAL_GATEWAY_MODE=http`，直接 `rabbitmq` 等未支持 mode 会在 handoff 前 fail closed。
- `cd frontend && npm run build`：通过，AI Trading runtime 摘要显示 Sessions 计数和 Attempts 计数/结果分布，Recent 区块显示 active/archived agent sessions，并能按当前 session 过滤 specs/signals；agent-session detail 页显示 handoff attempt 计数和 read-only attempt audit rows；`agent_session_archived` blocker 在信号行、handoff title、session detail 和 attempt 审计文案中会显示为可读 `Agent session archived`；当前策略卡和 recent spec rows 会优先使用 record payload 中的 `agent_session.status` 判断归档状态，在 selected/owning session archived 时禁用 save/approve/adjust/model-adjust/backtest/signal-preview 等写动作；failed 但仍 eligible 的 signal 会显示 `Retry ready`，tooltip 显示 `Retry handoff`；只保留 inspect/detail/audit 读操作；剩余为既有 browserslist/baseline/chunk-size warning。
- `/api/ai-trading/runtime`：已返回 current-user 非敏感 `model_adjustment` readiness；可显示 DeepSeek/Qwen provider/model 是否 ready、credential 是否存在、blockers 和安全 `next_actions`，且不返回 API key/base URL。前端 AI Trading runtime 摘要新增 `Model` 卡片，model-adjust 按钮优先以后端 `model_adjustment.ready` 为准，并把 missing profile/key/unsupported provider/missing model/missing endpoint 等 blocker 显示为可读状态/tooltip/下一步；Model 卡片的 icon-only setup 按钮会打开现有 LLM Config modal，不新增 API-key/base-URL 渲染面；LLM Config 保存成功后会同时刷新 Hyper AI profile 和 AI Trading runtime/records，避免用户配置 DeepSeek/Qwen 后 Model 卡片或 model-adjust gate 停留在旧状态。
- Playwright smoke 已打开 `http://127.0.0.1:5174/app/ai-trading`，跳过本地 onboarding 且未输入 API key；页面渲染 AI Trading runtime cards：`Gateway available / 15m max / Local mock`、`Model / Profile missing`、`Specs 92 / 90 backtest ready`、`Signals 90 / 0 ready`、`Attempts 88 / 88 submitted`；点击 `data-testid="ai-trading-model-config-button"` 后打开现有 `Hyper AI 配置` LLM config modal，包含 provider/API-key 字段。console 剩余为既有静态资源 404 与无关 `ContactDialog` ref warning。
- `/api/ai-trading/runtime`：已返回 current-user counts-only `agent_sessions.context_budget`，包含 active/archived、with/empty summary、max chars、near/over-budget、redacted/sensitive-looking counts 和 `secret_policy=counts_only_no_summary_text`；不会返回 `context_summary` 原文、Bob session locator、API key 或 token。前端 Sessions runtime 卡片现在按该字段显示 `ctx max` / `ctx watch` / `ctx over`。
- AI Trading runtime budget UI source guard 已纳入聚合回归：`tests/test_ai_trading_frontend_readiness_source.py` 现在同时检查 Settings locator UI 和 Hyper AI AI Trading Sessions runtime 卡片，证明 runtime budget label 只读取 counts-only budget 字段，不从 `context_summary` 原文渲染。
- V1 completion audit 现在要求一键本地验收脚本包含 `tests/test_ai_trading_frontend_readiness_source.py`，并要求 status 文档保留 `AI Trading runtime budget UI source guard` / `AI Trading env-check runtime context budget gate` Done 标记；如果有人从 aggregate regression 中移除 UI source guard，本地完成度审计会失败。
- 一键本地验收脚本现在把 `ai_trading_v1_completion_audit.py --strict-local` 输出写入临时 JSON，并结构化断言 `local_v1_accepted=true`、`ready_for_live_orders=false`、`github_upload=deferred_by_user_request`、`git_governance.status=accepted`、当前分支正确和 `local_blockers=[]`。
- 一键本地验收脚本现在会先跑 `scripts/local-dev/install_launch_agent.sh` 同步 LaunchAgent runtime mirror，再用 `ai_trading_v1_env_check.py --strict --require-runtime-mirror-current` 做默认 24 次、每次 5 秒的冷启动重试；可用 `AI_TRADING_RUNTIME_READINESS_ATTEMPTS` / `AI_TRADING_RUNTIME_READINESS_SLEEP_SECONDS` 调整等待窗口，runtime mirror 仍只写 `.hyperalpha-runtime-sync.json` 中的分支、commit、source tree digest 和同步时间，不写 env、API key、token 或其它凭据。
- DeepSeek/Qwen model-adjust 现在会把当前 agent session 的非敏感 `agent_session_id`、`agent_session_name`、`context_summary`、`context_summary_chars`、`summary_max_chars` 作为压缩上下文审计放入模型 prompt 和返回的 `model_context`；敏感上下文会变成 `[redacted_sensitive_context]`，超长上下文在 service 层也会截断到 `summary_max_chars=2000`，并继续标记 `ai_order_placement=disallowed`，不创建 signal event、不 handoff、不下单。
- In-app Browser 可以打开 `http://127.0.0.1:5174/#settings`；本地 auth config disabled 时 admin tab 按设计隐藏，普通本地浏览器不能看到 admin readiness 面板。管理员登录态下的 visual check 留到真实 Auth/JWKS 配置后验收。
- In-app Browser 可以打开 `http://127.0.0.1:5174/app/ai-trading`；跳过本地 onboarding 后可渲染 Hyper AI / AI Trading 页面、Gateway/Specs/Signals runtime、`Gateway available / 15m max / Local mock`、All/Crypto/HIP-3 市场分段和 Crypto/HIP-3 标的。
- In-app Browser 已验证 runtime agent context budget UI：`/app/ai-trading` 跳过本地 onboarding 后显示 `Sessions 47 active` 与 `ctx watch 1`，Recent agent sessions 仍显示单条 session `ctx 0 / 2000`，页面无 visible error，未输入 API key，未触发 handoff。
- In-app Browser 已验证 agent-session budget UI：`/app/ai-trading` 跳过本地 onboarding 后显示 `Context 0 / 2000`，Recent agent sessions 显示 `ctx 0 / 2000`；详情页 `/app/ai-trading/sessions/ait%3Abtc%3A4d53a40f6789` 显示 `Summary chars 0 / 2000` 和 `redacted, no credentials`。
- In-app Browser 已验证 agent-session UI：页面显示 `Sessions 2 active`、`Agent session` 下拉默认选中最新 `BTC V1 Live Stack Acceptance ...`、`Session name` / `Context summary` 编辑框、save/archive 图标按钮、`Recent agent sessions`、recent spec `#17` 和 recent signal `#15`。
- In-app Browser 已验证 agent-session history/context UI：选中 session 后 Recent 区块切换为 `Session specs` / `Session signals` 并只显示当前 session 的 spec `#17` / signal `#15`；`Load context` 会把 `ai_trading_agent_session_context.v1` 非敏感 context packet 回填到聊天输入；本地归档 smoke session 可在 `Archived sessions` 区块显示。
- In-app Browser 已验证 agent-session context compression UI：`Compress context` 会把当前 session 的 specs/signals/handoff blocker 压缩成可读 `AI Trading session compressed context v1` summary，写回 `Context summary` 输入框，且不会触发 `[redacted_sensitive_context]`。
- In-app Browser 已验证 agent-session full-page detail UI：`/app/ai-trading/sessions/ait%3Abtc%3A1d03c72b5168` 可直接加载 current-user session context，显示 status/symbol/spec/signal/handoff summary、`Session specs`、`Session signals`、`ai_order_placement=disallowed`，并能从主 AI Trading 页面通过 `Open session detail` 返回/进入。
- In-app Browser 已验证 AI Trading runtime Attempts 总览：跳过本地 onboarding 后 `/app/ai-trading` 显示 `Attempts`、`29 / 29 submitted`、`submitted:29`；未输入 API key，未触发 handoff。
- In-app Browser 已验证 HIP-3 分段过滤：`xyz:NVDA` / `xyz:AAPL` / `xyz:TSLA` 可见，BTC 不在 HIP-3 过滤结果中。
- In-app Browser 已验证 `xyz:NVDA` safe prompt fill 和 strategy draft：UI 显示 `NVDA · 15m`、`ready_for_review`、`Boundary signal only`、`Backtest not_run`、`Unsaved draft`。
- In-app Browser 已在最终代码验证 BTC 当前卡片完整安全流：draft -> natural-language adjust -> save -> approve -> inline backtest evidence -> `backtest ready` -> signal preview -> reject；证据记录为 spec `#5`、signal `#3 rejected`。
- In-app Browser 已验证 Recent signals 展示正向 mock handoff 结果：`BTC · buy`、`#2 submitted`，并可读取 handoff attempts，attempt `submitted`、`gateway_ready=true`、gateway response summary 已脱敏。
- 本地 live API 已在最终代码验证正向 mock gateway handoff：spec `#6`、signal event `#4`、`handoff_status=submitted`、latest attempt `submitted`、`gateway_ready=true`。
- Hyper AI AI Trading 当前策略卡片已改为内联 Backtest ID / Metrics JSON 输入，避免依赖浏览器原生 prompt；recent spec 行仍保留 prompt 兼容入口。
- Hyper AI AI Trading 用户动作请求已加 45s 超时恢复，避免一次网络/热更新抖动让 draft/save/approve/adjust/backtest/signal/handoff/reject 按钮永久 loading。
- LaunchAgent 本地后端已加入 `HYPERALPHA_LOCAL_DEV_LIGHT_MODE=true`，本地常驻服务跳过行情/新闻/账户快照等重后台采集，保留 AI Trading API、mock gateway 和前端验收稳定性。
- LaunchAgent `com.hyperalpha.ai-trading-local` 已固化成本地 runtime mirror 启动方式：安装脚本同步 runtime 副本到 Application Support，并排除 Vite 可变 cache `frontend/node_modules/.vite`，launchd 已在本机会话内恢复 frontend `5174`、backend `8802`、mock gateway `5621`，env checker 返回 `ready=true`；最新 gate 还要求 `.hyperalpha-runtime-sync.json` 的 source tree digest 与当前代码一致，避免前端/后端服务仍跑旧 runtime 副本。

## 当前未验收

- 实际 macOS 整机重启后的自动恢复还未物理验收；当前已完成 LaunchAgent 会话内自恢复验收。后续代码变更需要重跑 `scripts/local-dev/install_launch_agent.sh` 同步 runtime 副本。
- 真实 DeepSeek/Qwen API key live model-adjust 未验收；当前已新增本地 live model-adjust acceptance runner 和默认拒绝门，mocked Qwen regression 已通过，但还未在真实用户 Hyper AI profile/API key 下带 `--confirm-live-model-call` 验收。
- 真实 HyperAlpha 订单后端 URL/token live handoff 未验收；当前为 disabled-by-default 和 mock gateway contract 验收。
- 生产 handoff readiness gate 和生产总 readiness gate 已实现，但真实 HTTPS 订单后端 URL/token、真实 Auth/JWKS、硬风控生产值和 `AI_TRADING_PRODUCTION_HANDOFF_APPROVED=true` 的 live 验收未做。
- Settings Admin production readiness 面板代码和 build 已完成；真实登录态/真实 Auth 配置下的可视化验收未做。本地 auth disabled 时 admin tab 隐藏是预期状态。
- AI Trading agent session 后端/API/UI 已完成 first-class create/update/archive、当前 session 选择、归档列表、context packet 载入、context compression、按 session 过滤 specs/signals，以及独立 full-page session 详情页；真实生产登录态下的 session 详情页验收另行处理。
- 真实交易所执行不属于 V1 本地验收完成条件，必须另开生产实盘验收。

## V1 通过标准

- 一键本地验收 runner 通过：`scripts/local-dev/run_ai_trading_v1_local_acceptance.sh --confirm-local-mock-handoff`。
- V1 完成度边界审计通过：`cd backend && uv run python scripts/ai_trading_v1_completion_audit.py --strict-local`；生产实盘切换审计 `--strict-production` 必须在真实外部验收前保持失败；真实外部验收材料必须按 `docs/hyperalpha/ai-trading-v1-production-evidence.template.json` 脱敏填写并通过 `--production-evidence-file` 校验，且填好的 evidence 文件必须放在代码仓库外或私有 ops evidence 位置，repo 内文件不能解锁 live ready；只能使用模板定义的 root/item 字段，`items` 只能包含模板列出的七个外部验收项，不能追加任意原始输出字段或未知验收项；root `evidence_run_id` 必须是本次 production acceptance attempt 的唯一脱敏 run id，12-80 字符，只能使用字母、数字、dot、underscore、colon 或 hyphen；root `cutover_approval_ref` 必须包含同一个 run id，每个 item `artifact_refs` 都必须同时包含同一个 run id 和该 item id，且同一条 artifact ref 不能在同一 item 内重复或跨 item 复用；root `notes` 必须是可选、短文本列表，不能塞入原始日志、模型输出、trace 或运维 dump；每项 `validated_by` / `evidence_summary` 必须是具体、非 placeholder 且长度受限的验收说明，不能塞入大段原始日志或模型输出；`generated_at`、`expires_at` 与每项 `validated_at` 必须是带时区的 ISO-8601 时间戳，`generated_at` 不能早于任何 item `validated_at`，`generated_at` 与每项 `validated_at` 不能超过当前时间 300 秒以上，每项 `validated_at` 不能比 `generated_at` 早超过 7 天，`expires_at` 必须晚于 `generated_at`、未过期且不超过 `generated_at` 后 7 天；root `cutover_window.start_at/end_at` 必须包含当前生产审计时间且窗口不超过 8 小时；root `cutover_approval_ref` 必须指向本次实盘切换窗口的脱敏审批记录；`artifact_refs` 必须非空、每项最多 5 个、每条最多 300 字符，只能使用 `https://`、`ops://`、`lark://`、`notion://` 安全引用，不能嵌入 credentials、不能指向本机/私网/保留 IP。
- 后端 AI Trading 回归通过。
- 生产总 readiness 默认阻断且测试通过：`cd backend && uv run python scripts/ai_trading_v1_production_readiness_check.py --strict`。
- API-level V1 smoke runner 通过：`cd backend && uv run python scripts/ai_trading_v1_acceptance_smoke.py`。
- 环境 readiness 可检查：`cd backend && uv run python scripts/ai_trading_v1_env_check.py`。
- 本地 live-stack mock handoff 可复跑：`cd backend && uv run python scripts/ai_trading_v1_live_stack_acceptance.py --confirm-local-mock-handoff`。
- 前端 build 通过。
- Postgres/Snapshot DB 启动后，后端 `/api/ai-trading/runtime` 可响应。
- 浏览器从 `/app/ai-trading` 完成至少一条测试策略流：draft -> adjust -> save -> approve -> attach/run backtest evidence -> create signal event -> reject 一条 signal -> confirm handoff 一条 eligible mock gateway signal。
- Handoff payload 符合 `docs/hyperalpha/ai-trading-signal-gateway-contract.md`。
- Mock gateway 可用：`cd backend && uv run uvicorn dev_ai_trading_signal_gateway:app --port 5621 --host 127.0.0.1`，主后端 `AI_TRADING_SIGNAL_GATEWAY_URL` 指向 `http://127.0.0.1:5621/api/ai-trading/signals`。
- Status 和 memory 文档标记通过证据。
